# 联智中枢（LZCore）核心架构与系统设计

> **本文档为平台唯一有效的技术架构与工程设计规范。**
> 任何历史迁移、阶段性清理与弃用记录请参阅 `docs/CODE_CLEANUP_20260831.md`，严禁将历史废弃规范作为现行接口或实现的架构依据。

---

## 1. 架构哲学与核心设计目标

联智中枢（LZCore）的设计出发点，是解决大语言模型（LLM）在面对复杂多步骤企业基础设施运维时常见的“无约束幻觉、隐蔽误操作、黑盒执行与状态失真”痛点。

传统的智能体架构往往倾向于让大模型自由操纵代码解释器或裸脚本，导致权限不可控、执行轨迹不可溯、系统崩溃无法复原。**LZCore 确立了“认知推理与执行治理严格分离”的设计范式**：
- **认知层（Cognitive Layer）**：由大模型作为推理内核，负责解析运维人员意图、多轮推理分解、推导证据缺口、规划工具调用并根据客观反馈自主决策下一步行动。
- **治理层（Governance Layer）**：由平台坚固的底层运行时系统接管所有外界交互。运行时接管所有工具的 Schema 强校验、调用方租户身份隔离、Skill 资源切片授权、破坏性操作拦截、敏感数据动态脱敏、网络状态对账与全生命周期操作审计。

模型在治理框架内拥有自主推理的灵活性，但绝无可能绕过安全边界产生非受控副作用。

---

## 2. 端到端执行链路与单事实源（SSOT）模型

### 2.1 拓扑数据流

```text
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 接入层 (User Interaction)                                              │
 │ 现代响应式 Web UI (Zustand 状态机) / RESTful 接口 / WebSocket 实时双工 │
 └───────────────────────────────────┬────────────────────────────────────┘
                                     │ /api/agent/message & WebSocket 流
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 会话管理与运行时接入 (AgentApp)                                         │
 │ 鉴权认证、workspace_id 租户校验、长历史因果上下文装配                  │
 └───────────────────────────────────┬────────────────────────────────────┘
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 单事实源引擎 (SSOTRuntimeEngine)                                       │
 │ 驱动核心状态机 TaskState，记录权威生命周期与认知阶段                   │
 └───────────────────────────────────┬────────────────────────────────────┘
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 认知推理循环 (QueryLoop)                                               │
 │ · 多轮模型交互与证据聚合                                               │
 │ · plan_goal_ids 驱动证据补全                                           │
 │ · 目标驱动的只读自愈 (runtime_recoveries)                              │
 └───────────────────────────────────┬────────────────────────────────────┘
                                     │ 受控工具提案
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 治理执行网关 (ToolRuntimeClient)                                       │
 │ · Manifest 严格校验 (core/tools/manifest_registry.py)                  │
 │ · Caller Gate 鉴权与 Skill 资源切片核定                                │
 │ · 命令语义分类 (command_semantics)                                     │
 │ · 外部审批扩展拦截 (approval extension checkpoint)                     │
  │ · 执行（Shell 在宿主机并拦截破坏性命令；Python 按隔离策略选 Docker 或拒绝） │
 │ · 动态脱敏总线 (Redaction) 与 审计记录入库                             │
 └───────────────────────────────────┬────────────────────────────────────┘
                                     │
                                     ▼
 ┌────────────────────────────────────────────────────────────────────────┐
 │ 持久化存储与结果投影                                                   │
  │ 记录存储（默认文件，生产可用 PostgreSQL）│ 对象存储 │ 审计 │ AgentResult │
 └────────────────────────────────────────────────────────────────────────┘
```

### 2.2 权威状态与对外投影

- **权威事实源（`TaskState`）**：单次任务的所有执行细节（包括用户目标、执行计划、当前阶段、工具调用日志、证据索赔与断言状态）均由服务端 `TaskState` 权威记录。
- **对外统一投影（`AgentResult`）**：无论是 HTTP 轮询、SSE 还是 WebSocket 推送，面向前端和调用方的所有数据均派生自 `AgentResult`。平台拒绝在前端或代理层维护所谓的“第二套状态同步副本”，杜绝因前端缓存导致的状态不一致。

---

## 3. 任务目标结果与工具执行结果的严格正交解耦

在企业级运维中，一次工具调用的成败绝不能草率等同于用户任务的成败。LZCore 在核心类型体系中明确分离两类状态指标：

```text
       ┌─────────────────────────────────────────────────────────┐
       │   用户任务最终结果: execution_outcome                   │
       │   [ complete | partial | blocked | failed | unknown ]   │
       └────────────────────────────┬────────────────────────────┘
                                    │
                                    │ (逻辑独立、正交解耦)
                                    │
       ┌────────────────────────────┴────────────────────────────┐
       │   单次工具尝试结果: tool_execution_outcome              │
       │   [ success | error | timeout | rejected | unknown ]    │
       └─────────────────────────────────────────────────────────┘
```

1. **`tool_execution_outcome`（工具执行结果）**：
   客观记录单次工具调用的物理执行结果。无论网络闪断、命令超时还是参数校验失败，均如实记录。
2. **`execution_outcome`（用户任务终态）**：
   评估最终是否达成了运维人员的原始诉求：
   - **替代证据链自愈**：若首选工具发生异常（如远程只读接口超时），只要模型随后通过合规的备用手段（如天气工具或本地缓存分析）获取到了充分可靠的只读证据，整体任务依然判定为 `complete`。
   - **外部写入未知的防重放机制**：对于配置下发、网络修改等写操作，若因连接中断导致写入结果未知，平台保持 `unknown` 状态，严禁盲目自动重试（防止不可逆的配置二次污染）。系统保留该状态等待主动回读（Read-back）或人工对账确认。
   - **重启收敛机制**：若后台进程重启且该任务既没有耐久化关联资源、也没有活跃的校验器接管，外部操作账本会将其安全收敛为 `indeterminate` 终态——如实沉淀“结果当前无法确定”的历史事实，拒绝伪装成仍处于执行中的假死状态。

---

## 4. 工具边界、治理网关与纵深防御

平台实行双网关统一治理：外部系统与审批流程接入通过 `ToolRuntimeClient.invoke()`，模型编排与运行时节点通过 `ToolRuntime.execute_node()`。两个执行网关底层统一以 `core/tools/manifest_registry.py` 为权威 Manifest 注册表与契约中心，严格校验调用方身份（`allowed_callers`）、参数 Schema 与权限。

```text
外部调用 / REST / Approval  ──> ToolRuntimeClient.invoke()  ──┐
                                                             ├──> Manifest 契约校验 (manifest_registry.py)
模型编排 / TurnRunner / Agent ──> ToolRuntime.execute_node() ──┘    ├──> allowed_callers 准入校验
                                                                   ├──> Fail-Closed dry_run 防护
                                                                   ├──> 权限与 Skill 核定
                                                                   ├──> 执行器调用 (Executor)
                                                                   ├──> 动态数据脱敏 (Redaction)
                                                                   └──> 审计与账本写入 (Ledger/Audit)
```

### 4.1 运行时防护核心契约
- **双网关统一契约**：无论是大模型自主编排还是外部 API 驱动，均禁止绕过治理网关直接调用 handler。`allowed_callers` 显式声明允许调用该工具的角色（如 `turn_runner`、`human_review`、`orchestrator`、`system_job`），未经声明的调用方将被网关直接拦截。
- **内部实现隐藏**：底层 `handler_id` 仅用于服务端内部模块路由，严禁向大模型、前端工作台或开放 API 泄露，防止针对底层处理器的恶意逆向利用。
- **调用级 `dry_run` 安全防护**：默认关闭并拒绝执行未授权尝试。即使工具显式标记了 `dry_run_supported`，执行器也绝不调用任何具备副作用的写入逻辑，仅生成无害的配置预览；在未支持 `dry_run` 的工具上请求 dry_run 时，执行层实施 Fail-Closed 熔断阻断。公开端点 `/api/tools/dry-run` 仅输出参数校验与策略元数据，绝不触碰工具真实 Handler。
- **命令语义服务端强分类**：针对网络运维工具 `network.operations.device.manage`，命令是只读（`read`）还是配置下发（`configure`）由服务端依据真实命令语法字典与 AST 进行严密判定，**绝不信任模型在参数中自行声明的 `action` 字段**，坚决堵死模型通过伪造 `action=read` 绕过审批或触发非法重试的漏洞。
- **文件路径越界防护**：`workspace.file` 工具内部内置沙箱校验，严禁使用 `..` 相对路径、符号链接或绝对路径逃逸出当前指定的 `workspace_id` 物理目录。
- **代码与脚本执行边界**：`exec.run` 针对 Shell / PowerShell 命令在宿主机上执行，但实施严格的高危破坏性指令拦截；针对 Python 脚本，单机回环开发默认以本机子进程运行（best-effort），仅在配置或检测到强隔离策略（非回环监听、身份认证启用或显式开启强隔离）时强制调度 Docker 容器，当容器环境不可用时实行安全闭环（Fail-Closed）直接拒绝执行。

---

## 5. 目标驱动的自主容错与证据恢复机制（Goal-Driven Recovery）

平台拒绝设定“固定重试 N 次即失败”的僵化规则，转而采用先进的目标驱动自愈范式（`goal_loop`）：

### 5.1 恢复目标与证据索赔
- 当遇到可恢复的只读异常时，QueryLoop 自动生成结构化的只读恢复目标。
- 模型可以在后续迭代中通过 `plan_goal_ids` 将替代工具调用与该恢复目标关联。
- **防伪造完成校验**：模型声明关联并不代表目标达成。运行时引擎会严密校验替代调用的能力类别、资源凭据、以及证据类型（Evidence Kind）、真实事实（Fact）与目标（Target）的一致性。只有最终成功且终止的只读观察，才被允许关闭恢复目标。
- **独立容错**：某一设备的网络异常只影响该设备的目标达成度，绝不波及同批次其他健康设备的正常状态。参数错误、网络闪断或工具轮次均不会成为运行时草率终止 Agent 的借口。

### 5.2 确定性无进展拦截（Stall Detection）
虽然没有累计墙钟时限，但引擎内建确定性停滞检测：
- 若模型连续多次抛出完全相同的错误工具调用提案；
- 若底层驱动或 Provider 明确返回永久拒绝语义；
- 若所需外部证据已被系统权威判定为客观不可获取。
此时系统将状态优雅转换为结构化 `blocked`，保留完整证据切片并清晰告知运维人员堵塞卡点，避免无意义的死循环 Token 消耗。

### 5.3 观测与基线生命周期（Observation vs Reference）
`core/runtime_engine/context_contract.py` 严格规定了观测与基准的因果分级：
- **观测（Observation）永不自称“正常”**：单次巡检结果仅代表特定采样时间点的物理事实，禁止在未经历史基线对比前直接打上“正常”标签。
- **状态流转**：初次巡检产生 `candidate`（候选态）；只有经过运维工程师显式审阅确认后，方可升级为 `confirmed`（权威基准态）；后续更新将使同范围旧基线进入 `superseded`（已取代），异常基线可标记为 `invalidated`（已作废）。

---

## 6. 细粒度资源授权与审批流拦截扩展

### 6.1 领域 Skill 的最小权限原则
- 平台不预设静态全域超级权限，能力粒度绑定至已发布的领域 Skill。
- 每次工具调用前，服务端实时核验该 Skill 处于激活状态、调用工具在声明白名单内、且目标设备连接被收录在该 Skill 范围中。
- 设备底层账号权限是命令下发的最终仲裁者。超出 Skill 授权范围的调用将被结构化拒绝并反馈给模型。

### 6.2 零旁路变更审批扩展（Approval Extension）
当特定关键 Skill 启用 `approval_enabled=true` 时，系统激活零旁路审批流：
1. **调用冻结**：当模型生成配置下发（`configure`）指令时，核心执行拦截器（`execution_interceptor`）就地捕获该提案，完整持久化当前的 QueryLoop 状态快照（Checkpoint）。
2. **零硬件接触**：在审批通过前，任何配置指令绝不流向物理或虚拟网络设备。
3. **二次有效性核验**：运维主管在审批中心核准后，服务端重新核验目标设备在线状态与 Checkpoint 的有效性，复用原有的 `ToolRuntimeClient` 管道执行。
4. **无缝因果恢复**：执行结果原样回填至保存的 Checkpoint，QueryLoop 在原逻辑节点继续推理，前端无需构造虚拟的用户输入去“假装继续对话”。

---

## 7. 数据隔离、凭据安全与记忆写入网关

### 7.1 物理级租户隔离
任何跨文件、跨知识库或跨任务的底层数据读写，必须在请求上下文与存储层严格校验 `workspace_id`。工作区目录在物理磁盘上实行强隔离，彻底防范跨租户脏数据渗透。

### 7.2 工业级凭据信封加密（Secret Store）
- 本机单节点与分布式部署中的敏感密钥（API Key、SNMP Community、设备 SSH 私钥、认证令牌）由 `storage/secret_store.py` 统一接管。
- 底层采用经行业验证的高强度对称加密算法 `cryptography.fernet.Fernet` 进行信封加密。
- 主加密密钥由安全的外部环境变量 `LZCORE_MASTER_KEY` 或保护权限的 `LZCORE_MASTER_KEY_FILE` 注入。明文密钥严禁进入系统日志、Trace 链路或前端传输 Payload 中。

### 7.3 记忆防护网关（MemoryWriteGate）
长效记忆机制不是无序的聊天记录备份，而是经过提炼的运维认知资产：
- 所有长期记忆沉淀必须通过 `MemoryWriteGate` 网关校验，针对来源可信度、有效范围、生命周期 TTL 以及冲突情况进行仲裁。
- 只有与当前 `workspace_id` 匹配、状态处于 active 且未过期的经验或规则，才允许在后续会话中被语义索引召回。
- 外部输入、知识库与工具输出均被打上 `data_only` 标记，仅作为模型推理的数据依据，严禁当作执行指令执行。

---

## 8. 前端工作台职责边界与全链路可观测性

### 8.1 前端设计哲学
前端采用基于 React 18 + Vite 的现代化架构，依托 **Zustand** 构建高性能全局状态机，负责渲染交互式工作台、动态网络拓扑画布、白板演示层、日志流与任务中心。
- **前端绝对无权越狱**：前端仅负责呈现与收集意向，严禁在浏览器端自行判定权限、伪造任务终态、合成恢复目标或将本地缓存假定为实时权威事实。

### 8.2 全链路可观测性集成
平台原生支持云原生可观测性技术栈：
- **实时追踪**：接入 OpenTelemetry 规范，每一次用户请求贯穿模型推理、工具节点、审计日志的全链路 Trace ID 追踪。
- **事件总线**：在生产部署 Profile（`deployment/compose.production.yml`）中，系统配置 `LZCORE_EVENT_BUS_MODE: redis`，利用 Redis 高性能 Pub/Sub 支撑分布式异步事件广播。
- **监控指标与告警**：通过 `/metrics` 端点原生暴露 Prometheus 监控指标，开箱即用支持 Grafana 仪表盘监控与 Alertmanager 阈值告警。
