# 联智中枢（LZCore）工程协作与开发契约

> **本规范面向在此代码仓库中协同开发的人类高级工程师与自动化 AI 编程智能体。**
> 任何向本仓库贡献代码、设计接口或优化架构的行为，必须严格服从本契约所确立的工程规范与安全准则。

---

## 1. 命名与品牌尊严契约（Branding & Naming SSOT）

平台拥有统一的单一品牌与工程命名规范，禁止混淆或擅自引入陈旧命名：

| 适用语境与场景 | 标准标识符 | 规范说明 |
| :--- | :--- | :--- |
| **用户界面与外部交付** | **联智中枢** | 用于前端工作台标题、系统弹窗、报告制品及向最终用户展示的产品文档。 |
| **底层框架与技术架构** | **LZCore** | 用于核心技术方案、架构白皮书、类型命名空间（如 `LZCoreApp`）。 |
| **系统实体与部署工程** | **lzcore** | 仅用于 Git 仓库名、Docker 镜像名、配置文件（如 `lzcore.env`）、系统指标与日志文件名。 |

> **红线禁止项**：严禁恢复或在注释、代码、测试用例中引入任何已废弃的历史内部代号、陈旧工具别名或兼容垫片路径。

---

## 2. 绝对不可逾越的七大安全与架构红线

在对平台进行任何增删改动时，以下七项基本原则具有最高优先级，任何优化方案若违背以下原则将被 CI 流程与安全网关坚决否决：

```text
 ┌────────────────────────────────────────────────────────────────────────┐
 │                      七大工程与安全不可逾越红线                         │
 ├────────────────────────────────────────────────────────────────────────┤
 │ 1. 内核领域中立      │ 严禁在 core/ 中硬编码特定行业领域逻辑，行业能力一律插件化 │
 │ 2. 工具零旁路治理    │ 所有工具必须经过 ToolRuntimeClient.invoke()，严禁裸调 Handler │
 │ 3. 租户强隔离        │ 所有数据交互必须严格校验并绑定 workspace_id，禁止越权访问 │
 │ 4. 结果正交解耦      │ 严格区分任务成败与工具成败；未知写入严禁盲目自动重试       │
 │ 5. 权限实时动态核验  │ 每次设备配置前服务端重新核对 Skill 授权切片与设备凭据     │
 │ 6. 零敏感数据外溢    │ 凭证/密码绝不入库、不上日志、不进 Trace、不送前端持久化    │
 │ 7. 代码事实驱动文档  │ 文档必须与现行代码逻辑绝对一致，严禁逆向削足适履伪造代码   │
 └────────────────────────────────────────────────────────────────────────┘
```

1. **内核领域中立性**：
   平台核心（`core/` 与 `backend/`）保持纯粹的计算、调度与治理中立；所有行业特定对象（如网络设备协议、厂商 CLI 语法、行业巡检规则、专属业务图元）必须封装于 `extensions/` 独立插件模块中。
2. **工具零旁路治理**：
   任何工具执行必须经过 `ToolRuntimeClient.invoke()` 统一网关。严禁在控制器或业务逻辑中直接 `import` 内部 Handler 函数进行裸调用，绕过 Manifest 校验、Caller Gate 鉴权、参数脱敏与审计记录追踪。
3. **工作区租户强隔离**：
   所有涉及物理文件、制品存储、会话上下文与向量记忆的读写操作，必须显式传递并核验已授权的 `workspace_id`；前端工作台无权代表服务端编造默认或全局工作区。
4. **状态正交与写操作防重放**：
   严禁将单次工具异常等同于任务失败；只读异常可由模型结合 `goal_loop` 寻找替代证据达成任务。对于网络或主机的外部配置写入，若执行结果未确定（`unknown`），绝不自动执行可能导致不可逆破坏的二次重试，必须如实记录事实并留待回读核验。
5. **权限与授权动态裁决**：
   网络设备操作权限由服务端在每次执行瞬间依据当前生效并发布的 Skill 动态判定；设备底层账号是命令执行的终审裁决者。
6. **零敏感信息外溢防护**：
   API 密钥、SSH 私钥、SNMP Community、用户密码及受保护的环境凭证，严禁以明文形式提交至 Git、打印到控制台日志、附加至 Trace 追踪链路或存储在浏览器 LocalStorage 中。
7. **事实驱动文档一致性**：
   技术文档是系统物理代码与架构的镜像映射。修改功能代码时必须同步升级对应文档；绝不允许为了迎合陈旧、错误的文档而降低系统现行的安全标准或篡改核心契约。

---

## 3. 源码拓扑架构与权责归属

```text
HTTP / WebSocket 请求
  ├── backend/               ── API 路由、身份认证、WebSocket 通道与中间件
  ├── agent/                 ── 会话状态管理、运行时上下文聚合与能力展示
  ├── core/runtime_engine/   ── SSOT 引擎、QueryLoop 认知闭环、目标自愈与断言门禁
  ├── core/tools/            ── 17 个 Canonical 通用工具实现、Manifest 注册表与治理策略
  ├── extensions/            ── 领域专属扩展插件（网络运维、人工审批流等）
  ├── storage/               ── 租户存储边界、Fernet 凭据保险库与文件持久化
  ├── artifacts/             ── 巡检报告、拓扑数据与大文件结构化制品仓储
  ├── jobs/                  ── 内置 Local Worker 轮询与跨平台文件锁异步作业队列
  ├── observability/         ── Prometheus 监控采集、Grafana 仪表盘与 Trace 链
  └── frontend/              ── React 18、原生 CSS Tokens、Zustand、拓扑白板与流式交互工作台
```

### 关键目录职责隔离
- `core/runtime_engine/`：包含 `SSOTRuntimeEngine`、`QueryLoop`、上下文装配器与证据断言器。该目录是全平台的认知中枢，对任务状态拥有唯一裁决权。
- `core/tools/`：管理平台受控的 Canonical 工具集。任何工具的入库必须伴随完整的 JSON Schema 声明与安全策略配置。
- `storage/`：数据安全第一防线。物理目录 `workspaces/`、`logs/` 与 `config/providers/` 属于环境级私有数据，已严格加入 `.gitignore`，绝不可作为代码提交。
- `frontend/src/stores/`：基于 Zustand 管理浏览器端视图交互状态，严格遵守只读与展示原则，不得在客户端模拟或越权预先判定服务端业务逻辑。

---

## 4. 增量研发与演进检查矩阵

在向主分支提交任何 Pull Request 或变更前，必须对照本矩阵逐项核对：

- [ ] **工具变更**：若新增或调整通用工具，同步更新 `canonical_registry.py`、`manifest_registry.py`、安全策略类与自动化单元测试。
- [ ] **API 变更**：若调整或增加后端路由，确保在 `docs/API.md` 中更新契约表格，并在 `backend/api/` 中保持端点规范，绝不暴露私有未注册路径。
- [ ] **扩展变更**：严格保证扩展仅使用其命名空间下的专用路由与工具，遵循 `extensions/` 独立隔离规范。
- [ ] **状态机完整性**：对于状态流转逻辑，完整自测包含“创建、轮询、异常重试、人工取消、确定性终态、异步恢复”全流程。
- [ ] **文档与路径自洽**：核实修改涉及的所有相对路径、配置键名、环境变量与命令行参数均真实存在并可执行。

---

## 5. 多层级自动化验证防护网

本地开发验证必须全绿方可通过交付：

```bash
# 第一道防线：文档与运行时表面因果一致性核查 (SSOT 规范)
python scripts/verify_docs_runtime_consistency.py

# 第二道防线：核心认知循环与架构规范单元测试
.venv/bin/pytest -q harness/test_goal_loop.py harness/test_docs_consistency_script.py harness/test_runtime_prompt_ssot.py

# 第三道防线：全量核心契约测试套件
.venv/bin/pytest -q harness/

# 第四道防线：前端工作台测试与生产编译
cd frontend && npm test -- --run && npm run build
```

---

## 6. 生产交付契约与观测准则

- **严谨客观的技术表达**：在任何技术沟通、系统日志或回复中，严禁将“本地单元测试跑通”武断表述为“底层物理设备、生产网络或远程服务已经验证完毕”。
- **端到端生产部署验证**：标准的交付验收必须包含“本地单元测试全绿 -> Git 干净提交 -> 推送远端分支 -> Docker Compose 部署 -> 生产健康检查与实际拓扑端点验证”。
