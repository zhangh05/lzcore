# 目标驱动 Loop Engineering

## 目的

QueryLoop 向模型提供完整工具结果与可审计的恢复信息，由模型判断普通观察是否仍需补充。一次工具失败不自动成为任务必须完成的要求；扩展明确声明的领域证据目标由运行时按事实核对。

## 每轮流程

```text
用户请求与受治理上下文
  -> 模型产生工具调用或最终答复
  -> runtime 校验计划、结构性容量和依赖
  -> ToolRuntimeClient 执行
  -> 标准化 ToolResult / evidence claims
  -> 关闭目标、创建恢复目标，或向模型发送运行时 nudge
  -> 最终 gate 校验覆盖与终态
```

模型通过工具完成目标；服务器负责参数 schema、调用方身份、已选 Skill 的工具与设备连接范围以及取消信号。网络设备命令的最终权限由设备账号决定；平台没有危险命令策略或配置写入开关。可选的 `approval` 扩展不是权限门：仅当 Skill 主动开启时，它暂停一条已冻结的 `configure` 调用，等待外部决定。

## 可恢复与不可恢复

| 情况 | 行为 |
| --- | --- |
| 普通只读工具失败 | 记录建议性的恢复目标；模型依据任务需要选择补证据或结束，失败本身不否决最终答复 |
| 扩展声明了明确的领域证据恢复目标 | 按证据类型、事实与资源范围核对恢复结果 |
| 参数或 CLI 语法被拒绝 | 返回结构化反馈，由模型改正参数、选择 canonical fact 或查询权威资料 |
| 单个目标或设备连接失败 | 返回结构化结果；其余独立目标继续 |
| Skill 范围外调用或取消 | 返回结构化结果；模型据此继续或结束 |
| 外部写入结果未知 | 作为完整事实返回模型；模型自行决定 read-back、继续配置、重试或说明状态 |
| 已确认写失败 | 返回完整失败事实；模型自行决定下一步 |
| 已开启审批的配置调用 | 冻结精确操作并释放进程资源；整组决定终态后由服务端恢复原 checkpoint，以原 call id 回注完整结果 |

## 目标、证据与结构性边界

每个目标记录来源调用、资源范围、失败类别、可用的安全策略和尝试状态。`plan_goal_ids` 只是关联提示，不是完成证据：显式关联仍须通过工具、动作和资源目标兼容性校验；同一能力的隐式恢复也不得跨越 `device_id`、`connection_id`、`resource_id` 等资源身份。成功且已经终止的只读结果才可提交正向 evidence claim，失败、执行中或结果不确定的调用不能关闭目标。领域证据继续按 evidence kind、fact 与 target 匹配；参考文档可帮助修正命令，但不等于实时设备证据。

通用 `tool_recovery` 的尝试次数包含原始失败，仅用于可观测性；新出现的设备或目标不继承其他目标的历史。失败、参数校验、连接异常、命令数量和模型轮次都不会由运行时终止 Agent；完整结果持续交给模型选择下一步。生产 Agent 任务不因累计运行时间、模型轮次或工具节点计数而终止。

长任务轮询同样没有累计墙钟截止时间，仍尊重工具声明的终态、轮询次数和用户取消。单次 provider、连接和工具调用可以返回自身协议超时，这些是供模型继续推理的工具事实，不会被投影为 `total_time_exceeded`。

模型输入和输出不使用运行时 token 预算裁剪：完整会话历史、工具结果、编排证据和最终综合 evidence 都按原样传给模型。provider 返回未完成的长度/流中断标记时，QueryLoop 保留已接收内容并要求模型在同一会话继续，不把部分内容作为最终答复发送。

## 子 Agent

子 Agent 与父 Agent 使用同一套 canonical tool runtime。profile 只定义分工、提示词角色和输出合同，不作为工具白名单或权限降级。父会话选择 Skill 时，创建子 Agent 前服务端重新解析该 Skill，并把父会话实际可见的设备、连接和该 Skill 的工具范围传入子运行时；子 Agent 不能自行伪造或扩大该范围。子 Agent 没有总运行时间预算，用户取消才会终止正在运行的委派。

## 扩展合同

扩展返回结构化 `ToolResult`。领域无关的安全只读替代仍可通过 `runtime_recoveries` 描述，但必须通过完整合同校验，且不得作为新权限、凭据、资源、写命令或脚本的载体。厂商 CLI 语义差异由网络扩展发布为 `model_recovery_guidance`，不转换为运行时自动调用；模型保留调查路径和命令选择权。QueryLoop 仍是唯一模型循环，扩展不得维护第二个 LLM retry loop。

`execution_interceptor` 是扩展在真实工具执行前的领域无关钩子。它只能返回“继续”或“暂停这一条精确调用”，不能修改模型参数、更换工具或扩大 Skill 范围。暂停状态是 `waiting_external_input`，不是任务成功或失败。`approval` 扩展使用该钩子保存 prepared operation 和完整 QueryLoop checkpoint；外部决定后经 `ToolRuntimeClient` 执行原参数，服务端将真实/拒绝/失效结果按原 `call_id` 回填 checkpoint，并自动恢复同一逻辑循环。它不靠前端构造新的“继续”消息。

## 对外结果

`tool_execution_outcome` 保存工具尝试事实；`execution_outcome` 保存用户目标。存在失败工具卡不必然是任务失败：若替代证据满足目标，任务可以 `complete`。写入未知在所属运行进程存活期间保持为事实记录，模型可在后续任何一步决定是否用 read-back/reconcile 收集新事实。后端重启时，仍有耐久任务关联的记录继续跟踪；没有任何关联恢复源的前进程记录收敛为 `indeterminate`，保留审计事实但退出实时未决队列。

普通 `tool_recovery` 记录不参与最终答复拦截。它记录失败尝试及恢复进展，不把每次探索性工具调用转化为用户的额外要求。例如网页读取失败后，模型已通过天气工具取得所需预报，可以直接完成答复；未恢复的网页读取仍如实记录为 pending，不伪造成功。对应断言保留实际状态并标记 `required=false`，不参与任务最终断言结果汇总。明确的领域证据目标继续由 `recovery_final_gate` 核对。

## 回归要求

运行时或扩展恢复改动至少覆盖：参数纠错、替代证据、目标关联、预算耗尽、多目标隔离、未授权调用、写入未知与最终结果投影。网络扩展还应覆盖真实驱动读操作与多设备失败隔离。
